import styled from 'styled-components';

export const CanvasContainer = styled.div`
  flex: 1;
  position: relative;
`;

export const AreaGraph = styled.canvas`
  z-index: 0;
  position: relative;
  width: 1000px;
  height: 315px;
`;
